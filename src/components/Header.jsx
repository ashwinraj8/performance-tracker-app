// src/widgets/Header.jsx
import React from 'react';
import './Header.scss'; // Optional: create a corresponding SCSS file for styles

const Header = () => {
    return (
        <header className="header">
            <h1>Company Dashboard</h1>
        </header>
    );
};

export default Header;
